{
  "name": "rig-dashboard",
  "version": "1.28.0",
  "description": "Gaming rig stats dashboard for secondary display",
  "scripts": {
    "prepare:sidecar": "dotnet publish sensor-sidecar/sensor-sidecar.csproj -c Release",
    "build:egui": "cargo build --release --manifest-path src-egui/Cargo.toml",
    "build": "npm run prepare:sidecar && npm run build:egui",
    "test": "vitest run",
    "lint": "eslint frontend/renderer",
    "lint:fix": "eslint frontend/renderer --fix",
    "lint:md": "markdownlint-cli2 \"*.md\"",
    "fmt:rs": "cargo fmt --manifest-path src-egui/Cargo.toml",
    "fmt:rs:check": "cargo fmt --manifest-path src-egui/Cargo.toml -- --check",
    "clippy": "cargo clippy --manifest-path src-egui/Cargo.toml -- -D warnings",
    "verify": "npm run prepare:sidecar && cargo test --manifest-path rigstats-backend/Cargo.toml && cargo clippy --manifest-path src-egui/Cargo.toml -- -D warnings && npm run fmt:rs:check && npm run lint && npm run lint:md && vitest run",
    "prepare": "lefthook install"
  },
  "keywords": [],
  "author": "Daniel",
  "license": "MIT",
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "eslint": "^9.0.0",
    "globals": "^15.0.0",
    "lefthook": "^2.1.4",
    "markdownlint-cli2": "^0.21.0",
    "vitest": "^2.1.9"
  }
}
