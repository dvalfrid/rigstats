{
  "name": "rig-dashboard",
  "version": "1.22.2",
  "description": "Gaming rig stats dashboard for secondary display",
  "scripts": {
    "prepare:sidecar": "dotnet publish sensor-sidecar/sensor-sidecar.csproj -c Release",
    "start": "set PATH=%USERPROFILE%\\.cargo\\bin;%PATH% && tauri dev",
    "build": "npm run prepare:sidecar && set PATH=%USERPROFILE%\\.cargo\\bin;%PATH% && tauri build",
    "test": "vitest run",
    "lint": "eslint frontend/renderer",
    "lint:fix": "eslint frontend/renderer --fix",
    "lint:md": "markdownlint-cli2 \"*.md\"",
    "fmt:rs": "cargo fmt --manifest-path src-tauri/Cargo.toml",
    "fmt:rs:check": "cargo fmt --manifest-path src-tauri/Cargo.toml -- --check",
    "clippy": "cargo clippy --manifest-path src-tauri/Cargo.toml",
    "verify": "npm run prepare:sidecar && set PATH=%USERPROFILE%\\.cargo\\bin;%PATH% && cargo test --manifest-path src-tauri/Cargo.toml && cargo clippy --manifest-path src-tauri/Cargo.toml -- -D warnings && cargo check --manifest-path src-tauri/Cargo.toml && npm run lint && npm run lint:md && vitest run",
    "prepare": "lefthook install"
  },
  "keywords": [],
  "author": "Daniel",
  "license": "MIT",
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "@tauri-apps/cli": "^2.0.0",
    "eslint": "^9.0.0",
    "globals": "^15.0.0",
    "lefthook": "^2.1.4",
    "markdownlint-cli2": "^0.21.0",
    "vitest": "^2.1.9"
  },
  "dependencies": {
    "@tauri-apps/plugin-opener": "^2.5.3"
  }
}
