{
  "name": "rig-dashboard",
  "description": "Tool dependencies — build is handled by cargo xtask",
  "scripts": {
    "prepare": "lefthook install"
  },
  "devDependencies": {
    "lefthook": "^2.1.4",
    "markdownlint-cli2": "^0.21.0"
  }
}
