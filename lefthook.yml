pre-commit:
  commands:
    fmt-rs:
      glob: "{src-egui,rigstats-backend}/**/*.rs"
      run: cargo xtask fmt-check
    clippy:
      glob: "{src-egui,rigstats-backend}/**/*.rs"
      run: cargo xtask clippy
